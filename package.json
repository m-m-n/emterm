{
  "name": "emterm",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot serve.ts",
    "icons": "./scripts/generate-icons.sh",
    "prebuild": "./scripts/generate-icons.sh",
    "build": "bun build src/main.ts --outdir dist --minify && cp src/index.html dist/ && cp src/styles.css dist/ && sed -i 's/main.ts/main.js/g' dist/index.html",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/cli": "^2.9.6",
    "@types/bun": "latest",
    "@wdio/cli": "^9.22.0",
    "happy-dom": "^20.0.11",
    "typescript": "^5.0.0"
  }
}
