feature: mux-vertical-tabs
collected_at: "2026-07-20T23:55:10+09:00"
session_ids:
  - 417e05e9-2005-4a89-9c9c-07f1b58641de
  - 503a8fd2-67e1-4456-8800-2cd011e3f8c4
note: >-
  Sixth develop pass (task0011: press on the overlay card no longer
  starts a terminal selection — the MouseInput press guard now routes
  through the shared point_in_sidebar helper covering both placements).
  Round-1..5 signals preserved below; round-6 signals appended.
signals:
  review_critical_high:
    # round 1
    - stable_id: aff70644e3bdd29f
      category: comprehensive
      file: src-tauri/src/ui/mux_sidebar.rs
      title: "オーバーレイサイドバーがタイトルバーの最小化/最大化/閉じるボタンを覆う"
      resolution: fixed
    - stable_id: a683038081ce469c
      category: spec
      file: src-tauri/src/window_host.rs
      title: "Sidebar visibility changes resize every PTY"
      resolution: deferred
    # round 2
    - stable_id: a6671519566d1452
      category: comprehensive
      file: src-tauri/src/window_host.rs
      title: "スクロールバーのクリック抑止領域が persistent サイドバー幅ぶんズレる（task0006 右寄せの副作用）"
      resolution: fixed
    # round 3: critical/high なし（medium 1 件はレビュアー自身が最終検証で
    # 非到達と判断して取り下げ — round3.yaml 参照）
    # round 4: critical/high なし（findings ゼロ — round4.yaml 参照）
    # round 5: critical/high なし。medium 2 件（codex:spec のみ検出、
    # round5.yaml 参照 — padding 帯のホイール dead-zone は unresolved、
    # round-1 deferred 項目 a683038081ce469c の再指摘は re-deferred）
    # round 6: critical/high なし（5 レビュアー全員 findings ゼロ —
    # round6.yaml 参照。middle-click paste は pre-existing で対象外と明記）
  conflict_reworks:
    - {task: task0005, retries: 1}
  file_prediction_misses:
    - task: task0003
      files: [src-tauri/src/settings.rs, src-tauri/src/settings_window/commands.rs]
    - task: task0005
      files: [src-tauri/src/settings.rs, src-tauri/src/ui/mux_sidebar.rs, src-tauri/src/ui/mod.rs]
  verification_failures: []
  discretionary_perspectives:
    - perspective: architecture
      reason: "task0006 が grid/cursor/search の origin 計算を横断変更するため（round 2）"
  declined_findings: []
lessons_candidates:
  - >-
    「サイドバーを左→右に移す」という一見小さな UI 変更が、CentralPanel の
    右端に依存する別コンポーネント（右端スクロールバーのクリック抑止ガード）
    を巻き込んで high 級リグレッションになった。egui SidePanel は
    CentralPanel を縮めるが、winit 生イベントを幾何で手動ガードしている
    箇所（top/bottom strip / scrollbar）はその縮小を自動追従しない。
    「ウィンドウの右端」を絶対座標で参照している全ガードは、サイドバーの
    inset を反映する必要がある。geometry を触る変更では
    「絶対 window 端に依存する hit-test の棚卸し」をレビュー観点に加える
    価値がある。
  - >-
    right-edge 配置は left-edge より geometry がシンプルになった（x-origin
    項が全廃され width-only inset に）。ただし副作用として、以前 x-origin に
    inset を足していたヘルパー mux_sidebar_x_inset が dead API 化し、名前
    （x_inset）が実挙動（width-only）と矛盾する medium が残った。配置変更で
    「不要になった旧経路のヘルパー」を消し忘れやすい。
  - >-
    2回目の develop 差分再実行では、workflow.yaml の後段 step
    (implement/review/verify/retrospect) を pending に戻し、追加タスク
    (task0006) を tasks に足し、要件マッピングを更新する手当てが必要だった。
    upstream 3 step (spec/design/plan) は「変更した内容を反映して completed
    のまま commit を現行 HEAD に更新」で回せた。差分再実行のための
    workflow.yaml 手当ては手動で、定型化の余地がある。
  - >-
    integration worktree の検証環境準備（gitignored フォント + 非追跡
    bun.lock の happy-dom ピン揃え）は round 1 と同じ手当てが再び必要だった。
    develop 再実行でも同じ落とし穴を踏むので、verify 前の環境準備は
    チェックリスト化する価値がある。
  - >-
    round 3 でも同じ環境準備 2 点（fetch-fonts.sh + bun.lock ピン揃え）を
    三たび踏んだ。integration worktree 作成直後に機械的に流す前処理として
    完全に定型化できることが確定した。
  - >-
    レビュアーが最初の報告後に自主継続して指摘を取り下げるケースがあった
    （負の高さ edge case → 最小ウィンドウ制約で非到達と自己判定）。同一
    task-id から複数回の完了通知が来る場合、最後の構造化出力を正とする
    運用で問題なかった。
  - >-
    task0007 の実機バグ（カード背景が右/下で潰れる）はユニットテストを
    すり抜けた。原因はテストフックが「計算した rect」を記録していて
    「実際に描かれる rect」を見ていなかったこと。egui の Frame::show は
    コンテンツ実寸で背景を描くため、意図した rect と実描画は別物になり得る。
    描画系のテストは意図値でなく実描画値（paint rect）を捕捉するのが正。
    round 4 で test-catches-bug 検証（修正を戻してテストが落ちることの確認）
    まで行う運用が有効だった。
  - >-
    実機スクショ付きのバグ報告 → 修正ラウンドは、デザイン変更ラウンド
    （DESIGN.md 更新あり）と違い create-spec/design を触らず create-plan
    からのリセットで回せた。モックが正でコードが誤りのケースは
    「実装バグ修正タスク」として plan に直行するのが最短だった。
  - >-
    egui の描画不具合 2 連発（Frame::show の content 追従 / Area の
    per-id サイズキャッシュ + grow-only set_min_size）はどちらも
    「フレームワークが保持する暗黙状態 vs 毎フレーム計算した意図値」の
    乖離クラス。egui で幾何を固定したい面は、キャッシュを経由しない
    fresh Ui（allocate_new_ui + max_rect）に載せるのが確実で、テストは
    単一フレームでなく「サイズを変えた連続フレーム」で書く必要がある。
  - >-
    winit 手動ガードと egui パネル幾何の乖離クラス（round 2 の教訓）が
    ホイールルーティングで的中した。今回は hit-region 純関数を描画側と
    同一導出に集約する設計を plan 段階で固定でき、実装は AC-4 で
    「実際のパネル合成順に対する一致テスト」まで書けた。教訓が plan に
    反映されて再発防止として機能した例。
  - >-
    input-handling domain の宣言で floor に security が入り codex
    クロス検証が発火した round だった。codex:spec だけが medium 2 件を
    出し、claude 3 観点は 0 件 — クロスモデルは「見逃し拾い」よりも
    「境界ケースの別解釈」を出す傾向。codex-only confidence 50 の
    運用どおり記録して完了ゲートには影響しなかった。
  - >-
    round 2 の教訓（winit 手動ガードが egui パネル幾何に追従しない）は
    round 5 のホイール、round 6 の押下ガードと同一クラスで 3 度目。
    サイドバー導入時に「winit 側で幾何判定している全イベント種
    （wheel / press / middle-click / motion）」を一括で棚卸しして
    共有ヘルパーに寄せるタスクを最初から切っておけば、実機バグ報告
    2 往復を省けた可能性が高い。同種の UI 面追加時は plan 段階で
    イベント種の総当たり表を作る価値がある。
