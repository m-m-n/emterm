# Task Plan: task0002 — Backfill the prior feature's test-mapping rows

## Goal

Bring `feature-docs/mouse-report-reset-active-gesture/`'s test mapping into
agreement with reality: its TS-4 checkbox and its TS-4 / SC-C verification
rows point at this feature's window-free tests, while its real-hardware
acceptance criterion stays mapped to a manual scenario.

## Requirements

FR8

## Scope

### Files to Create

None. Both targets are existing documents.

### Files to Modify

- `feature-docs/mouse-report-reset-active-gesture/SPEC.md` — the TS-4 checkbox row is pointed at the new window-free tests, with the write-request caveat recorded
- `feature-docs/mouse-report-reset-active-gesture/VERIFICATION.md` — the TS-4 row and the SC-C row are pointed at the same tests, with the write-request caveat recorded and the real-hardware confirmation kept on a manual scenario

## Design

Read IMPLEMENTATION.md first: decision D1 (why this task does not wait for
task0001), decision D4 (a recorded sink call is evidence of a write request
only), and the Shared Components row that pins the test identifiers this task
cites. Do not read task0001's plan.

### What to cite

The rows cite, by identifier and module path:

- the end-to-end window-free test
  `focus_loss_cleanup_publishes_selection_without_a_window` in
  `src-tauri/src/window_host/tests.rs`, which asserts the drag flag being
  cleared, the pending anchor being consumed and returned, and the selection
  being published;
- the destination-predicate truth-table tests in the same module, whose names
  begin with `selection_publish_targets_`, for the destination rule the prior
  feature's TS-4 row depends on.

Cite the prefix for the truth-table tests rather than enumerating individual
names, so the rows stay correct however the eight rows are split across test
functions.

### What each row must say

1. The prior SPEC.md TS-4 checkbox row states that the scenario is now driven
   by the window-free tests named above, replacing the previous source-scan
   -only situation.
2. The prior VERIFICATION.md TS-4 row and SC-C row point at the same tests and
   state the same thing.
3. All three rows record, in words, that a recorded sink call is evidence that
   the publish path REQUESTED the write, not evidence of an OS-level primary
   selection or clipboard update.
4. Consequently the prior feature's AC-3 real-hardware confirmation stays
   mapped to its manual scenario and is nowhere presented as unit-verified.

### Editing discipline

This is a correction of existing rows, not a rewrite. Keep each document's
existing structure, heading set, table shape, identifier spellings and
checkbox conventions; change only the cells and the sentences the four points
above require, plus whatever minimal wording keeps the surrounding sentence
grammatical. If a row named by the SPEC (the TS-4 checkbox, the TS-4 row, the
SC-C row) does not exist in the shape described, stop and report a plan
deviation instead of inventing or restructuring rows.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1: The prior feature's SPEC.md TS-4 checkbox row names
      `focus_loss_cleanup_publishes_selection_without_a_window` and the module
      path `src-tauri/src/window_host/tests.rs`, and no other scenario row,
      requirement or section of that document is altered. (FR8)
- [ ] AC-2: The prior feature's VERIFICATION.md TS-4 row and SC-C row both
      name the same test and module path, and the truth-table tests are
      referenced by the prefix `selection_publish_targets_`. (FR8)
- [ ] AC-3: All three corrected rows state explicitly that a recorded sink
      call is evidence of the write REQUEST and not of an OS-level primary or
      clipboard update. (FR8)
- [ ] AC-4: The prior feature's AC-3 real-hardware confirmation is still
      mapped to a manual scenario in that feature's VERIFICATION.md, and the
      words "unit-verified" (or any equivalent claim) are not applied to it
      anywhere in either document. (FR8)
- [ ] AC-5: Only the two documents listed in Scope are modified — no source
      file, no test, no workflow file, and nothing under this feature's own
      directory. (FR8)

## Test Notes

There is no automated test for this task: both deliverables are document
corrections, so verification is by inspection of the two files. Make each
acceptance criterion mechanically checkable by a reader or a text search:

- AC-1 / AC-2: search each document for
  `focus_loss_cleanup_publishes_selection_without_a_window` and for
  `selection_publish_targets_`; each must appear in the rows named above and
  nowhere that would imply a different scenario is covered.
- AC-3: search for the request-versus-update wording in each corrected row.
- AC-4: confirm the manual scenario mapping for the prior feature's AC-3 is
  still present and unqualified.
- AC-5: the change set for this task is exactly two files.

The feature-level verification of these criteria is recorded as the manual
scenario in this feature's VERIFICATION.md.

## Out of Scope

- Any change to Rust source, tests or build configuration.
- Any change to this feature's own SPEC.md, REQUIREMENTS.md, IMPLEMENTATION.md
  or VERIFICATION.md.
- Re-verifying, re-running or re-judging the prior feature's other scenarios,
  or correcting rows the SPEC does not name.
- Rewriting the prior feature's documents for style, structure or
  completeness.
