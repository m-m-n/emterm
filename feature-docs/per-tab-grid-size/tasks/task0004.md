# Task Plan: task0004 — Correct the task0001 test-evidence red_confirmed record

## Provenance

- source: review
- review_round: 1
- source_ids: c9d1e2db0ce11e59

## Goal

The task0001 test-evidence record is internally consistent: its AC-2 entry's
`red_confirmed` flag agrees with its own `red_reason` narrative, follows the
same recording convention as the sibling task0002 record, and contains no
reference to a section that does not exist.

## Requirements

NFR2

## Scope

### Files to Create

- (none)

### Files to Modify

- `test-docs/per-tab-grid-size/task0001.tests.yaml` — the AC-2 entry's
  `red_confirmed` / `red_reason` fields.

## Design

The AC-2 entry currently states `red_confirmed: true` while its own
`red_reason` narrative says the red state could not be produced, and it
points to an `unconfirmed_reds` section that does not exist in the file. The
sibling record `test-docs/per-tab-grid-size/task0002.tests.yaml` records the
same situation as `red_confirmed: false`, which is the convention to align
to.

1. Set the AC-2 entry's `red_confirmed` to `false`.
2. Rewrite its `red_reason` to state plainly that the red state could not be
   confirmed for this criterion, and that the sibling task's tests (which
   did confirm red) guard the shared judgment criterion — keeping the
   existing factual content, dropping the contradiction.
3. Remove the dangling "Recorded here and in `unconfirmed_reds` below"
   reference (either drop the sentence or point at what actually exists).
4. Change nothing else in the file: no other entry, no key ordering churn,
   no reformatting beyond the edited entry.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (TS11): the AC-2 entry records `red_confirmed: false`, and its
      `red_reason` text contains no claim contradicting that flag. (NFR2)
- [ ] AC-2 (TS11): no reference to an `unconfirmed_reds` section (or any
      other nonexistent section) remains anywhere in the file. (NFR2)
- [ ] AC-3 (TS11): the file parses as valid YAML and every entry other than
      AC-2 is byte-identical to its state before this task. (NFR2)

## Test Notes

- This is a test-evidence record correction; no Rust test is added. AC-1 and
  AC-2 are verified by reading the edited entry; AC-3 by a YAML parse of the
  file and a diff confined to the AC-2 entry. TS11 (VERIFICATION.md) is the
  feature-level manual check that covers this consistency class.
- No cargo invocation is required, but the task must not break the tree:
  the file is not compiled into any build, so the standard build gates are
  unaffected by construction.

## Out of Scope

- Any change to `test-docs/per-tab-grid-size/task0002.tests.yaml` (it is
  already in the correct form and serves as the convention reference).
- Any change to source code or to the tests the record describes.
- Restructuring the tests.yaml schema or adding an `unconfirmed_reds`
  section.
